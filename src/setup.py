from setuptools import setup, find_packages

setup(
    name="ethicnet",
    version="0.0.1",
    description="",
    author="Pauline BERBERI, Cédric GOUBARD",
    packages=find_packages(), #où se situent les packages à importer : dans le dossier. remplace "find package in src"
)
